---
interface Props {
  message?: string;
}
const { message = '読み込み中...' } = Astro.props;
---
<div id="loading" style="text-align:center; padding:48px;">
  <div class="spinner"></div>
  <p style="color:var(--color-muted); margin-top:12px;">{message}</p>
</div>
